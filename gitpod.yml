tasks:
  - init: npm ci && npm run build
    command: npm run dev

github:
  prebuilds:
    addBadge: true
    addComment: false
    addCheck: true
    branches: true
    pullRequestsFromForks: true

ports:
  - port: 3000
    onOpen: open-preview